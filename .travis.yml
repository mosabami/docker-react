language: generic
sudo: required
services:
  - docker 

before_install:
  - docker build -t mosabami/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true mosabami/docker-react npm run test